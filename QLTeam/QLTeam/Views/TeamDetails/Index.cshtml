@model IEnumerable<QLTeam.Models.TeamDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Chi tiết team</h2>

<p>
    @*@Html.ActionLink("Create New", "Create")*@
</p>
<table class="table">
    <tr>
        <th>
            STT
        </th>
        <th>
            Team
        </th>
        <th>
            Cấp bậc
        </th>
        <th>
            Họ tên
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TeamDetailID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TeamID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CapBacName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HoTen)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TeamDetailID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TeamDetailID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TeamDetailID })
        </td>*@
    </tr>
}

</table>

<div>
    @Html.ActionLink("Back to List", "../Teams")
</div>
